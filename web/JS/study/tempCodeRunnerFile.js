const reverseStringArray = str.map(element => {
//     if (typeof element === 'string') {
//         let reversed = "";
//         for (let i = element.length - 1; i >= 0; i--) {
//             reversed += element[i];
//         }
//         return reversed;
//     } else {
//         return element.map(e => {
//             let reversed = "";
//             for (let i = e.length - 1; i >= 0; i--) {
//                 reversed += e[i];
//             }
//             return reversed;
//         });
//     }
// });
// console.log("Массив str с элементами в обратном порядке:", reverseStringArray);
